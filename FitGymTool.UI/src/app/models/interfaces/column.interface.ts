/**
 * Interface defining the structure for table column configuration.
 * Used to define column properties for data tables and grids throughout the application.
 */
export interface Column {
  field: string;
  header: string;
}
