/**
 * Data transfer object for membership status mapping information.
 * Represents the mapping between membership status IDs and their corresponding names
 * used to track and manage different states of gym memberships.
 */
export class MembershipStatusMappingDto {
  public id: number = 0;
  public statusName: string = '';
}
